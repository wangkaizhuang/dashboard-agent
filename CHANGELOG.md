# 更新日志 (Changelog)

本文件记录 Dashboard Agent 的重要改动。日期格式 YYYY-MM-DD。

## [未发布]

### 新增 (Added)

- **对话上下文分界 + 会话分叉**（见 `docs/superpowers/specs/2026-06-07-context-boundary-and-fork-design.md`）：
  - **无关检测**：在已有看板的会话里发新需求时，先做一次轻量相关性判断。若明显是新主题（如供应链会话里发"做变形金刚看板"），弹出选择卡：**基于现有继续 / 重新生成（忽略旧上下文）**。判定失败一律放行、不打扰。
  - **上下文分界**：选"重新生成"会在该消息打分界标记（存于 `Message.metadata.contextBoundary`，零迁移）。此后**喂给模型的上下文只取最近分界点之后**，但**界面始终展示完整历史**。
  - **会话分叉**：每轮结果下方「从此分叉新会话」图标 → 复制截止该节点的历史（含分界标记）+ 当前看板 + 步骤到新会话，并用**有效上下文做主题总结作为新标题**。
  - 新增 SSE 事件 `intent_choice`、接口 `POST /api/sessions/[id]/intent` 与 `/fork`、组件 `IntentChoiceCard`。

## [0.2.1] — 2026-06-07

### 修复 (Fixed)

- **F3 联动数据生成不稳定**（生成提示词）：此前部分生成不产出联动数据、或指标卡各档复制相同值（点筛选无变化）。现 `layout.ts` 要求被控组件必须同时包含指标卡与图表、且只要有维度就放非空 controls 的筛选器；`mockdata.ts` 加 ❌/✅ 反例强约束每个 controlledBy 组件（含指标卡）各变体数值必须不同。实测：4 个 KPI 各档值全部不同（如转化率 18.6%/22.8%/27.9%）、图表亦不同。
- **布局偶发过窄卡片**（生成提示词）：`layout.ts` 新增布局规则——每行 span 之和=12、单组件 span≥3、严禁 span 1/2。实测 span 分布 [3,4,6,8,12]，无过窄卡。
- **专家模式多问题无引导**：问题卡标题显示「问题 N/M」计数；多个问题涌入时聊天自动滚动到**第一个未回答问题**（顶部对齐），不再跳到底部切掉首题。
- **暂停恢复文案笼统**：SCORE_REPORT 恢复提示改为点名具体步骤（如「将从『Mock 数据』步骤自动重新生成」），不再是泛化的「当前步骤」。

## [0.2.0] — 2026-06-06

经过多轮「多 subagent 真机测评 → 裁判评判 → 修复 → 复验」迭代，本批改动覆盖生成质量、三大交互（F1/F2/F3）、移动端响应式、THINK 推理与一系列体验加固。全程 `tsc` 零错误、单元测试 158/158 通过。

### 修复 (Fixed)

- **质量评分误判导致流程卡死**：评分器此前只看输出前 3000 字，大输出步骤（布局 JSON / Mock 数据 / 模板 HTML）被当成「不完整」扣分（实测均分仅 56–60、部分 <30），导致流水线暂停卡住。现改为：节选量提至 8000 字、明确告知评分器「截断不扣分，只评已展示内容」、评分校准（能用即 ≥60）；并修复 `score || 75` 把合法 0 分错当 75 的 bug，分数 0–100 钳制。修复后各步稳定 88–96。
- **F3 组件联动完全失效**：`dcSwitch` 的定位逻辑与生成属性不匹配（查 `data-controlled-by`/`getElementById` 均命中不了），每次点筛选都静默无效。改为用每个组件都带的 `data-dc` 精确定位；新增 `injectLinkageFix` 在预览时把 `data-dc` 别名为 `id`，**存量模板**也能联动。
- **联动数据各档相同**：`mockdata` 提示词此前对变体值无约束，指标卡 day/week/month 复制同值 → 点筛选无变化。现强约束「各变体数值显著不同」并补指标卡示例。
- **低分模板图表塌陷成空白卡**：新增 `injectChartHeightFix`，预览时对可见但 0 高度的 ECharts 容器兜底设最小高度。
- **SCORE_REPORT 卡片竞态**：暂停时 `loadSession()` 早于服务端 finally 写库，偶尔丢卡。改为流关闭（`[DONE]`）后再刷新。
- **`contextKeepRecent` 死控件**：配置滑块此前不生效（pipeline 硬编码 10），现全链路接通。
- **局部更新残留标记**：`partial-update` 现剥离 LLM 误吐的 `dc:start/end` 注释，避免破坏后续替换。
- **真实会话加载误显示新建引导**：`MessageList` 现区分「真实会话加载中（显示加载态）」与「新建草稿（显示引导）」，硬刷新不再误显示「开始设计您的仪表板」。
- **注释只填备注无法发送**：注释备注本就是指令，现允许空输入框 + 注释直接发送（自动合成消息）。
- **生成失败静默无反馈**：失败时显示 toast 提示。
- **THINK 空输出卡死**：推理模型偶尔把 token 全用于思考、正文为空被评 0 卡住；现 THINK 步骤产出为空时自动回退普通调用补出内容。
- **全屏预览层与移动 sheet z-index 冲突**、**预览 iframe `loading="lazy"` 致切换后空白**：已修。
- **会话加载健壮性**：`loadSession` 失败重试一次 + 加载代次守卫（快速切会话时只有最新一次能写状态，杜绝串台/冲空）。

### 新增 (Added)

- **移动端 / 平板响应式布局**（两轮）：侧边栏在手机上变抽屉式覆盖层（汉堡开关 + 遮罩）；对话头部承载汉堡与「进度/预览」入口（< xl 全程可达，修复了平板 768–1279px 入口缺失盲区）；右侧进度/预览面板在移动端以全屏 sheet 呈现；配置抽屉移动端全宽。桌面端布局保持不变。
- **THINK 深思模式接入推理模型**：新增按模式分模型——THINK 用 `deepseek-v4-flash`（默认，可在配置抽屉切 flash/pro），其余模式仍用 `gpt-5.4-mini`；`think.ts` 捕获原生 `reasoning_content` 流并持久化，「查看推理过程」真正可见。
- **评分徽章 tooltip**：解释 0–100 分档含义。
- **专家模式引导**：问题卡提示「回答全部问题后将自动继续生成」；移动端生成中提示去「进度/预览」查看步骤。
- **文档**：新增 `README.md`（本地/生产/Docker 启动、环境变量对照表、`.env` vs `.env.local` 说明）与常用 Docker 命令。

### 备注 (Notes)

- 「客户端切会话偶发空白」经多种方式（硬刷新 / 快速硬切 / 10 次快速侧栏切换）均无法复现，判定为测试环境（坏实例 404 + dev 严格模式/预览工具）假象；仍加了上述加载加固作为防御。
- 生成出来的仪表板本身为桌面宽（≥1280px，设计如此），手机预览内可横向滚动查看。
